<h1>Editing endpoint</h1>

<% form_for(@endpoint) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @endpoint %> |
<%= link_to 'Back', endpoints_path %>