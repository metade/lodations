<h1>New query</h1>

<% form_for(@query, :url => idea_canned_queries_path(@idea)) do |f| %>
  <%= render(:partial => 'canned_queries/form', :object => f)%>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', idea_canned_queries_path %>
